# subscription-tracker

# Express Template
npx express-generator --no-view --git ./

# Install nodemon to restart the server when a change is made
npm install --save-dev nodemon

# Adding a linter to keep the code clean
npx eslint --init